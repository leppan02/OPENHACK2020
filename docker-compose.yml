version: "3.8"
services:
  web:
    build: .
    depends_on:
      - postgres
  postgres:
    image: postgres
    volumes:
      - db-data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=hh
      - POSTGRES_PASSWORD=docker
  nginx:
    image: nginx
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:80"
    depends_on:
      - web

volumes:
  db-data:
